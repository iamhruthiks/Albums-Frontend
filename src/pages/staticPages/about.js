// material-ui
import { Typography } from '@mui/material';
import MainCard from 'components/MainCard';

const SamplePage = () => {
  return (
    <MainCard title="Albums">
      <Typography variant="body2">About React!</Typography>
    </MainCard>
  );
};

export default SamplePage;
